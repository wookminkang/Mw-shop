

export default async function CategoryListPage ({searchParams} : { searchParams: string}) {
  const params = await searchParams;
  //console.log(`search pamras`, params)
  return <>...CategoryListPage</>
}